# User queries
query GetUser($id: ID!) {
  user(id: $id) {
    id
    firstName
    lastName
  }
}

# User mutations
mutation UpdateUser($input: UpdateUserInput!) {
  updateUser(input: $input) {
    id
    firstName
  }
}
