type Query {
  node(id: ID!): Node
}

interface Node {
  id: ID!
  firstName: String!
}

type User implements Node {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
}

type Admin implements Node {
  id: ID!
  firstName: String!
  role: String!
}
